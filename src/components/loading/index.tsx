import { ContainerLoading, TextLoading } from "./style";

const Loading = () => {
  return (
    <ContainerLoading>
      <TextLoading>LOADING ...</TextLoading>
    </ContainerLoading>
  );
};

export default Loading;
