export type Props = {
  routerActual?: string;
};

export type Routes = {
  users: boolean;
  admin: boolean;
  home: boolean;
  logout: boolean;
  products: boolean;
  plans: boolean;
  properties: boolean;
  configinitial: boolean,

};
